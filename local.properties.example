# Copy this file to local.properties (ignored by git) and fill in your own values.
# Every value can also be supplied as an environment variable with the same name.
DB_URL=jdbc:postgresql://localhost:5432/taskflow
DB_USERNAME=postgres
DB_PASSWORD=

# At least 32 characters. Generate one with: openssl rand -base64 48
JWT_SECRET=
